import 'package:flutter/material.dart';

class FiltersSettingsScreen extends StatelessWidget {
  static const routeName = '/filters-settings-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Filters"),
      ),
      body: Center(
        child: Text("filters screen coming soon"),
      ),
    );
  }
}
